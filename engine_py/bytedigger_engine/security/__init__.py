"""Packaged security assets: semgrep ruleset, secure-codegen prompt rules,
and the deterministic security-lint gate script. Resolved by
config_provider.default_security_asset in both checkout and pip layouts."""
