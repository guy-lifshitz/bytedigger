"""Placeholder module (bd#22, L2).

This module path is required by AC-C1 and AC-C4, which both force on
`import conformance.quant_lint`. Its body is bd#24's deliverable
(`lint_quantifier_completeness`, `Finding`, and the fixture-document
grammar in CONTRACTS_SPEC.md §1.6) and is intentionally not implemented
here — see spec `[G22:18]`. This module defines no public names and does
no work at import time.
"""
